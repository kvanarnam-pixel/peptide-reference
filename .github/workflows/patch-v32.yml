name: Patch v32

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/patch-v32.py
      - run: |
          git config user.name "Kevin"
          git config user.email "kevinvanarnam@gmail.com"
          git add app.js research/GHK-Cu_Compound_Research.md index.html service-worker.js
          git commit -m "Batch 2: GHK-Cu dose-gap + 4-12wk cycle; MOTS-c timing anytime. Cache-bust v32."
          git push origin HEAD:main
